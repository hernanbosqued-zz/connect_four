Hacer un .JAR 
-------------
rm -rf bin 
javac src/connect_four/*.java -d bin
jar cvfm aaa.jar ./src/META-INF/MANIFEST.MF -C ./bin/ .
java -jar aaa.jar

Hacer al .JAR ejecutable
------------------------

1) crear archivo stub.sh

#!/bin/sh
MYSELF=`which "$0" 2>/dev/null`
[ $? -gt 0 -a -f "$0" ] && MYSELF="./$0"
java=java
if test -n "$JAVA_HOME"; then
    java="$JAVA_HOME/bin/java"
fi
exec "$java" $java_args -jar $MYSELF "$@"
exit 1

2)cat stub.sh PROJECT.jar > PROJECT.run && chmod +x PROJECT.run



